# Core orchestration
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0
langchain-anthropic>=0.3.0
langchain-chroma>=0.1.4
langchain-community>=0.2.0
langchain>=0.3.0
langchain-core>=0.2.0

# Embeddings (local, no extra API key needed for the demo)
# fastembed uses ONNX runtime — avoids PyTorch's torch._dynamo import conflicts
fastembed>=0.3.0

# API layer
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# Data + validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Observability — Langfuse (optional — set LANGFUSE_SECRET_KEY + LANGFUSE_PUBLIC_KEY to activate)
langfuse>=2.0.0
